---
- name: Allow Loki HTTP port through firewalld (RedHat/Rocky/CentOS)
  ansible.posix.firewalld:
    port: "{{ loki_port }}/tcp"
    permanent: true
    state: enabled
    immediate: true
  when: ansible_os_family == "RedHat"

- name: Allow Loki HTTP port through ufw (Debian/Ubuntu)
  community.general.ufw:
    rule: allow
    port: "{{ loki_port }}"
    proto: tcp
  when: ansible_os_family == "Debian"
