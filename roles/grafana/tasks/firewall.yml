---
- name: Allow Grafana port through firewalld (RedHat/Rocky/CentOS)
  ansible.posix.firewalld:
    port: "{{ grafana_port }}/tcp"
    permanent: true
    state: enabled
    immediate: true
  when: ansible_os_family == "RedHat"

- name: Allow Grafana port through ufw (Debian/Ubuntu)
  community.general.ufw:
    rule: allow
    port: "{{ grafana_port }}"
    proto: tcp
  when: ansible_os_family == "Debian"
