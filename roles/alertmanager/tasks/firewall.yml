---
- name: Allow Alertmanager port through firewalld (RedHat/Rocky/CentOS)
  ansible.posix.firewalld:
    port: "{{ alertmanager_port }}/tcp"
    permanent: true
    state: enabled
    immediate: true
  when: ansible_os_family == "RedHat"

- name: Allow Alertmanager port through ufw (Debian/Ubuntu)
  community.general.ufw:
    rule: allow
    port: "{{ alertmanager_port }}"
    proto: tcp
  when: ansible_os_family == "Debian"
