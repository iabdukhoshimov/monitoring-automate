---
- name: Allow Prometheus port through firewalld (RedHat/Rocky/CentOS)
  ansible.posix.firewalld:
    port: "{{ prometheus_port }}/tcp"
    permanent: true
    state: enabled
    immediate: true
  when: ansible_os_family == "RedHat"

- name: Allow Prometheus port through ufw (Debian/Ubuntu)
  community.general.ufw:
    rule: allow
    port: "{{ prometheus_port }}"
    proto: tcp
  when: ansible_os_family == "Debian"
