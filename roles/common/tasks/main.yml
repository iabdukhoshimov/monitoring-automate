---
- name: Load OS-specific variables
  ansible.builtin.include_vars: "{{ ansible_os_family }}.yml"
  failed_when: false

- name: Import OS preparation tasks
  ansible.builtin.import_tasks: os_prep.yml
  tags: [common_os]
