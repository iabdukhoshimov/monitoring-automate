---
- name: Allow event_service port through firewalld (RedHat/Rocky/CentOS)
  ansible.posix.firewalld:
    port: "{{ event_service_port }}/tcp"
    permanent: true
    state: enabled
    immediate: true
  when: ansible_os_family == "RedHat"

- name: Allow event_service port through ufw (Debian/Ubuntu)
  community.general.ufw:
    rule: allow
    port: "{{ event_service_port }}"
    proto: tcp
  when: ansible_os_family == "Debian"
