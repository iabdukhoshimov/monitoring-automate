---
- name: Allow Alloy HTTP port through firewalld (RedHat/Rocky/CentOS)
  ansible.posix.firewalld:
    port: "{{ alloy_http_port }}/tcp"
    permanent: true
    state: enabled
    immediate: true
  when: ansible_os_family == "RedHat"

- name: Allow Alloy HTTP port through ufw (Debian/Ubuntu)
  community.general.ufw:
    rule: allow
    port: "{{ alloy_http_port }}"
    proto: tcp
  when: ansible_os_family == "Debian"
